# collector output (pre-anonymization, carries local paths/ids): never publish
raw_local/
scripts/raw_local/
# log-mirror service output: raw live-log copies (local paths/ids) — never publish
logs_mirror/
raw_snapshots/
# CDP watcher captures: raw client-perceived timing (local) — never publish
scripts/watcher/out/
# stray diagnostic exports or raw dumps dropped inside this repo folder
error_session*.md
raw_evidence/
transcripts/
# stray dumps tools may drop beside output
tmp

# local-only repo tooling (index + gates), not part of the publication
tools/
# general per-machine collector for Qoder installations: local use only
scripts/export_error_sessions.py

# regenerated by the python scripts; never part of the publication
__pycache__/
*.pyc

# operator's prior-art search log: deliberately NOT published (decision 2026-09-10);
# the citable content stands on its own in PUBLICATION.md section 2.5
prior-works.md

# release strategy, status notes and media plan: kept local by decision
# 2026-09-10. Everything a reader needs is in the published set instead -- CITATION.cff,
# the dataset card's Citation section, LICENSE, and paper_ai_QoS.md section 1.4.
PUBLICATION.md

# positioning advice from another model, with our adopted/declined record: working
# notes, not part of the publication (same treatment as PUBLICATION.md)
reposition.md

# local-only inputs and superseded working documents; they were excised from the
# published history on 2026-09-10 and must not be re-added
/methodology_guidance/
/ai_qos_methodology_docs.zip
/NESTING_WORKFLOW.md
